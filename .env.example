# Discord Bot Configuration
DISCORD_TOKEN=your_bot_token_here
CLIENT_ID=your_client_id_here

# Optional: Guild ID for guild-specific commands (faster deployment during development)
GUILD_ID=your_guild_id_here

# Groq API Configuration for transcription and summarization
GROQ_API_KEY=your_groq_api_key_here
GROQ_WHISPER_MODEL=whisper-large-v3
GROQ_SUMMARY_MODEL=llama-3.3-70b-versatile

# Caddy Reverse Proxy Configuration
# For development: use localhost (self-signed cert)
# For production: set to your actual domain (Let's Encrypt)
DOMAIN=localhost

# Docker User/Group Configuration
# Set these to match your host user to fix bind mount permissions
# Run: id -u && id -g to get your values
USER_ID=1000
GROUP_ID=1000